/*
  <Error text='text component:' onClick={this.onGetTiker}
*/
import React, { Fragment } from 'react'

const Error = (text, onClick) => {
  return <Fragment>
    {text} <button onClick={onClick}>Reloading</button>
  </Fragment>
}

export default Error;
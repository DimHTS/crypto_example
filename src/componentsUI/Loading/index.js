/*
  <Loading />
*/
import React, { Fragment } from 'react'

import Spinner from '../Spinner'

const Loading = () => {
  return <Fragment>
    <Spinner /> loading...
  </Fragment>
}

export default Loading;